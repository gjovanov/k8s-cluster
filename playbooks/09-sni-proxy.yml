---
# =============================================================================
# Phase 9: SNI Proxy (TURNS on port 443) + nginx2 upstream update
# =============================================================================
- name: "Phase 9: SNI Proxy for TURNS on port 443"
  hosts: localhost
  connection: local
  become: yes
  roles:
    - sni-proxy
