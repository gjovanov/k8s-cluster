---
# =============================================================================
# Phase 3: K8s Prerequisites - Install on all VMs
# =============================================================================
- name: "Phase 3: K8s Prerequisites"
  hosts: k8s_cluster
  become: yes
  roles:
    - k8s-common
